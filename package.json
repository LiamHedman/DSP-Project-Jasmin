
{
  "scripts": {
    "start expo": "expo start",
    "reset-project": "node ./scripts/reset-project.js",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web",
    "test": "jest --watchAll",
    "lint": "expo lint"
  },
  "dependencies": {
    "@types/ws": "^8.18.0",
    "axios": "^1.8.4",
    "express": "^4.21.2",
    "ws": "^8.18.1"
  }
}
